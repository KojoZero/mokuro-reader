diff --git a/node_modules/panzoom/index.js b/node_modules/panzoom/index.js
index cc1556c..5ffdabf 100644
--- a/node_modules/panzoom/index.js
+++ b/node_modules/panzoom/index.js
@@ -459,7 +459,7 @@ function createPanZoom(domElement, options) {
       return moveBy(dx, dy);
     }
 
-    if (moveByAnimation) moveByAnimation.cancel();
+    //if (moveByAnimation) moveByAnimation.cancel();
 
     var from = { x: 0, y: 0 };
     var to = { x: dx, y: dy };
@@ -934,7 +934,12 @@ function createPanZoom(domElement, options) {
   function getScaleMultiplier(delta) {
     var sign = Math.sign(delta);
     var deltaAdjustedSpeed = Math.min(0.25, Math.abs(speed * delta / 128));
-    return 1 - sign * deltaAdjustedSpeed;
+    var zoomInMultiplier = 1 + deltaAdjustedSpeed;
+    var multiplier = zoomInMultiplier;
+    if (sign > 0) {
+      multiplier = 1 / zoomInMultiplier;
+    }
+    return multiplier;
   }
 
   function triggerPanStart() {
